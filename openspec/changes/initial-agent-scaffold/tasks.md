## 1. プロジェクト基盤セットアップ

- [x] 1.1 Electron の Main/Renderer 最小構成を作成し、アプリ起動時にウィンドウが表示されることを確認する
- [x] 1.2 Main 側にエージェント実行モジュールの雛形を作成し、依存注入ポイント（設定・認証）を定義する
- [x] 1.3 Renderer 側に最小 UI（入力欄・送信・応答表示）を実装する

## 2. Agent Bridge 実装

- [x] 2.1 `submitPrompt` リクエストと `promptResult` レスポンスの IPC 契約を定義する
- [x] 2.2 Main で `pi-coding-agent` SDK を呼び出す実行パスを実装し、成功応答を Renderer に返す
- [x] 2.3 CLI 別プロセス実行を使わないことを実装とテストで担保する
- [x] 2.4 実行失敗時に標準化エラーを返却するハンドリングを追加し、UI で失敗理由を表示する

## 3. Heartbeat Jobs 実装

- [x] 3.1 Main に固定間隔ハートビートループを実装し、tick 発火ログを確認する
- [x] 3.2 ジョブ登録モデル（有効フラグ付き）を実装し、tick ごとの順次実行を実現する
- [x] 3.3 単一ジョブ失敗時でも後続ジョブを継続実行するエラー隔離を実装する

## 4. 検証と運用準備

- [ ] 4.1 `desktop-shell` / `agent-bridge` / `heartbeat-jobs` の受け入れシナリオを手動テストとして実施する
- [x] 4.2 ログ出力と最低限のデバッグ情報（IPC 入出力・ジョブ実行結果）を整備する
- [x] 4.3 音声機能を非対象とする境界を README と実装メモに明記する
