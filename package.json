{
  "name": "lilt-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Lilt-AI desktop shell with Pi SDK bridge",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && electron .",
    "test": "npm run build && node --test"
  },
  "dependencies": {
    "@mariozechner/pi-coding-agent": "latest"
  },
  "devDependencies": {
    "electron": "^31.7.7",
    "typescript": "^5.7.3"
  }
}
